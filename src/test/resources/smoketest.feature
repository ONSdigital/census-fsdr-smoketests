Feature: Check all services are running and accessible

    Scenario: Check Adecco is running
     When Adecco is running and accessible

    Scenario: Check ServiceNow is running
     When ServiceNow is running and accessible

    Scenario: Check AirWatch is running
     When AirWatch is running and accessible

    Scenario: Check postgres is running
     When postgres is running and accessible

    Scenario: Check the UI is running
     When the UI is running and accessible